name: nuconta_marketplace
description: A Flutter app where users spend their Nuconta balance on awesome products.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.10.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  graphql_flutter: 4.0.1
  http: 0.12.2

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
